# /mc-refine

> **Deprecated (v3).** Use `/mc-feature` continuous pipeline — clarify stage + `mc-prd` subagent.

Resume interrupted refinement only if migrating from v3. See `commands/legacy/README.md`.
