---
name: mc-upgrade
description: "Mission Control — safe kit upgrade. Refreshes orchestrator docs and skills without overwriting user specs. Usage: /mc-upgrade"
---

# /mc-upgrade

Safe kit upgrade. Refreshes orchestrator docs and skills without overwriting user specs, features, or `state.json`.

Invoke skill: `mc-upgrade`

Options: `--check` (is an update available?), `--dry-run` (preview changes).
