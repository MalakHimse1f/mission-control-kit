# Custom overrides

Files in this folder are **never overwritten** when you run `/mc-upgrade` or the installer update flow.

Use it for:

- Team-specific routing notes
- Local conventions the kit should not clobber
- Optional patches referenced by your orchestrator session

Kit-shipped docs live in the synced control root; your `features/` specs and this `custom/` folder are user-owned.
