# Tech stack workstream

Infrastructure and app setup — **not** user-facing UX.

| Path | Purpose |
|------|---------|
| `stack.json` | Project mode, platforms, frameworks (structured) |
| `CONTEXT.md` | Human-readable stack summary for agents |
| `{slug}/` | One setup item (e.g. `scaffold-nextjs`, `scaffold-ios-swift`) |

Copy `_template/` when adding a new tech item, or let `/mc-braindump` / `/mc-init` scaffold it.

Tech items: **spec → plan → build** (no layout/wireframes).

See `../WORKSTREAMS.md`.
