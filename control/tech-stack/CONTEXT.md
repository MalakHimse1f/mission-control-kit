# Tech stack context

Not established yet. Run **`/mc-init`** in a new chat.

Mission Control separates:

- **Tech stack** — app setup and configuration (scaffold Next.js, iOS project, CI, database, etc.)
- **Features** — what end users see and do (sign up, sign in, dashboard, etc.)

Each item gets its own spec, task list, and build track on the dashboard.
