export const version = '4.7.1-research-presentation';

/** No-op migration — documents orchestrator must present research HTML in chat (v4.7.1). */
export async function up() {
  /* See RESEARCH-LAYOUT.md → Present to the user. */
}
